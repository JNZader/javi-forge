kind: forge-template
contract_version: 0.1.0
template:
  id: template.api.go
  owner_repo: javi-forge
  status: slice-cutover-complete
  milestone: javi-forge-completion
  family: api
  variant: go
  supported_stacks:
    - go
  assets:
    - path: github/ci-go.yml
      role: Go API CI with golangci-lint and go test
    - path: github/dependabot-automerge.yml
      role: Dependabot auto-merge workflow
    - path: dependabot/header.yml
      role: Dependabot base header fragment
    - path: dependabot/github-actions.yml
      role: Dependabot GitHub Actions update fragment
  bootstrap_dependencies:
    - ci.bootstrap.local
    - lib/common.sh
  lineage:
    reference_sources:
      - https://github.com/JNZader-Vault/project-starter-framework/tree/main/.github/workflows
    source_type: curated-delta
    maintenance_owner: javi-forge
    active_external_dependency: none
    cutover_status: javi-forge-completion
    extraction_status: slice-forge-completion
  rules:
    - generation happens only through scripts/forge-init.sh
    - CI uses golangci-lint-action@v6 pinned to latest; consumers may pin to a specific version
  notes:
    - javi-forge-completion adds the first Go API stack-specific template.
    - Requires Go 1.23+, golangci-lint on the runner.
