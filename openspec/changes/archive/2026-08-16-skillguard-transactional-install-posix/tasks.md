# Tasks: SkillGuard Transactional Install/Repair (POSIX write path — Slice 3a)

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~915 (design File Changes table; tests + fixtures included) |
| 400-line budget risk | High (WU-3 transaction engine ~400 sits on the commit ceiling) |
| Chained PRs recommended | No (user decision: ONE PR to `main` under `size:exception`, four work-unit commits inside it) |
| Suggested split | One PR off current `main`; commits WU-1 → WU-2 → WU-3 → WU-4 |
| Delivery strategy | ask-on-risk |
| Chain strategy | stacked-to-main (Slice-1 runtime + Slice-2 recognition already on `main`, released in javi-forge@1.29.0) |

Decision needed before apply: No (the `size:exception` is a locked user decision, same disposition as Slice 2)
Chained PRs recommended: No
Chain strategy: stacked-to-main
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Est. lines | Boundaries |
|------|------|-----------:|------------|
| WU-1 | Settings write-plan helpers in `claude-hook-settings.ts` (+test): `planLegacyCohortExcision`, `planForceReplace`, `buildManagedContainer`, export `MANAGED_MATCHER` | ~145 | Start: `main` clean; Finish: pure-helper rows green, no filesystem; Rollback: restore the two files. Pure functions reusing `LEGACY_COHORT`/`deepStructuralEqual`; no I/O |
| WU-2 | `secure-fs-posix.ts` POSIX ACL/ownership adapters (+test): Linux `getfacl` / macOS `/bin/ls -lde`, `createPosixSecureFs`, `selectSecureFs` | ~210 | Start: WU-1 present; Finish: mocked-spawn refusal rows green (never skip), real-ACL fixtures skip only on incapable hosts; Rollback: delete the two files. No transaction core yet |
| WU-3 | `PlatformSecureFs` interface + `runTransaction` engine (`secure-fs-transaction.ts` +test) | ~400 | Start: WU-2's `PlatformSecureFs` shape stable; Finish: fake-adapter fault matrix green, host-independent, no root; Rollback: delete the two files. STOP-FOR-SPLIT if commit > 400 — split the fault-test file with its owning behavior into its own commit, never defer safety tests |
| WU-4 | Manager `_run` seam + public wrappers in `claude-hook-manager.ts` (+ `report` reconcile) (+test) | ~160 | Start: WU-1 helpers + WU-3 engine green; Finish: full state→action matrix through `_run` with a fake adapter green, real-adapter idempotency skip-only-on-incapable; Rollback: restore the two seams to their throwing stubs + revert `report` field |

## Explicit Gates (POSIX transactional write slice)

- [x] G.1 Base is exact current `main` (Slice-1 runtime MJS + manifest AND Slice-2 recognition/doctor/planners present). ONE PR to `main` under a pre-approved `size:exception` (~915 lines > 800, same disposition as Slice 2); each internal work-unit commit `< 400` changed lines. If WU-3 forecasts > 400 before its fault suite is complete, split the fault-test file with its owning behavior into an additional commit — flag it as a size decision, never compress transactional safety.
- [x] G.2 Strict TDD (`openspec/config.yaml` `strict_tdd: true`): every mutating behavior lands RED (failing test) → GREEN (smallest impl) → REFACTOR, tests committed WITH the behavior they cover.
- [x] G.3 NO filesystem mutation on ANY refuse or no-op path: no target write, backup, temp, rename, or directory creation on `foreign|symlink|non-regular|malformed|partial-legacy`, on `unsafe-parent-chain`/`unsupported-posix-acl` refusal, on Windows refusal, or on the `managed-current` no-op. `managed-current` is a zero-write no-op: bytes AND mtime preserved exactly, transaction never entered for that component.
- [x] G.4 Force rule keys on MATCHER EXACTNESS (parent contract §324 / JD-A-001), NOT unconditionally on sibling count: `matcherExact` → replace the marker-proven handler in place regardless of siblings; matcher edited AND `siblingHandlers === 0` → replace in place; matcher edited AND `siblingHandlers > 0` → refuse even under `--force`. `planForceReplace` exposes `matcherExact` + `siblingHandlers` so the manager enforces this precisely.
- [x] G.5 Backups are FORCED-ONLY (Decision 5 / JD-004): a persistent `<base>.javi-forge.bak.<ISO-ms>.<8hex>` is created ONLY for `repair --force` over `edited-managed`; routine `released-outdated`→upgrade and `exact-legacy`→migrate roll back from in-memory captured bytes via a transient rollback-temp and leave `backups[]` empty. `captureFile` opens `O_NOFOLLOW|O_RDONLY` (JD-B-003). Commit is asset-first, settings-second; guarded reverse-order rollback STOPS on lost proof / post-write hash drift.
- [x] G.6 Core matrix is host-independent (fake `PlatformSecureFs` through the `_run` seam) and NEVER platform-skipped (JD-002/JD-003). Adapter-refusal tests never skip; real-ACL fixtures + real-adapter `mkdtemp 0700` idempotency skip ONLY on incapable hosts. `clock`, `nonce`, `PlatformSecureFs`, and fs-fault points (temp-open, temp-write, fsync via `writeExclusive`, rename, second-target) are injectable — no root, no special host.
- [x] G.7 NO Windows `.ps1` helper / `installerHelpers.windowsSecureObject` manifest binding / `package:check` gate for it / `windows-latest` `manager` CI job (Slice 3b): `manifest.json` and `package.json` unchanged, no production dependency added. `selectSecureFs("win32")` → `null` → manager refuses `windows-secure-object-unavailable` with zero mutation. NO CLI dispatch / init wiring / effective-execution matrix / uninstall command (Slice 4). Slice-2 code grows in place; no classifier/planner logic relocated or duplicated.
- [x] G.8 `pnpm validate` (typecheck + typecheck:test + lint + test) + `pnpm test:coverage` (85 lines / 80 branches, thresholds never lowered) + `pnpm package:check` green before done. A post-apply Judgment Day will re-verify (JD-S1-FR3 findings pattern).

## Phase 1: WU-1 — settings write-plan helpers (RED → GREEN)

- [x] 1.1 **(WU-1, RED)** Extend `src/lib/claude-hook-settings.test.ts` with failing pure unit tests against crafted parsed values (no filesystem): `planLegacyCohortExcision` locates the exact four-object cohort via `LEGACY_COHORT`+`deepStructuralEqual`, returns ascending `removePreIndices`/`removePostIndices` + `insertPreAt`, and preserves every non-cohort sibling by index; `planForceReplace` returns `matcherExact:true` → in-place handler replacement preserving siblings, `matcherExact:false && siblingHandlers===0` → eligible in-place, `matcherExact:false && siblingHandlers>0` → `refused` with reason (§324); `buildManagedContainer(sha)` returns `{hooks:{PreToolUse:[managedGroup]}}` built from the current settings-entry SHA; `MANAGED_MATCHER` is exported and equals the exact managed matcher. Covers R1.S2, R1.S4, R5.S1. Verify: `pnpm vitest run src/lib/claude-hook-settings.test.ts` fails as specified.
- [x] 1.2 **(WU-1, GREEN)** Grow `src/lib/claude-hook-settings.ts` in place with the three pure helpers + their result interfaces (`LegacyCohortExcisionPlan`, `ForceReplacePlan`): `planLegacyCohortExcision(parsed)` (no `identities` param — JD-A-003; reuse `LEGACY_COHORT`+`deepStructuralEqual`, never re-derive `classifyLegacy`/`classifySettingsEntry`), `planForceReplace(parsed, currentAssetSha)` (resolve marker-proven `groupIndex`/`handlerIndex`, compare enclosing matcher to `MANAGED_MATCHER`, expose `matcherExact` + `siblingHandlers`, apply the §324/JD-A-001 eligibility rule), `buildManagedContainer(currentAssetSha)` (takes the SHA not the whole `Manifest` — JD-A-002 no settings→manager runtime cycle; any `Manifest` reference is type-only). Add/export `MANAGED_MATCHER` if missing. Verify: `pnpm vitest run src/lib/claude-hook-settings.test.ts`.

## Phase 2: WU-2 — POSIX ACL/ownership adapters (RED → GREEN)

- [x] 2.1 **(WU-2, RED)** Create `src/lib/secure-fs-posix.test.ts` (failing): mocked-spawn refusals that NEVER skip — Linux `getfacl` absent (ENOENT) / non-zero exit / timeout / named-user|group|`mask::`|`default:` line / changed output between preflight and commit all → `unsupported-posix-acl`; macOS `/bin/ls -lde` absent / non-zero / timeout / trailing `+` on the mode line / numbered ACE line → `unsupported-posix-acl`; ownership proof refuses group/other-writable or foreign-owned dir → `unsafe-parent-chain`; `selectSecureFs("win32")`/other → `null`. Plus real-ACL fixture cases (`setfacl`/`chmod +a`) guarded to skip only when the host lacks the capability. Covers R2.S1, R2.S2, R6.S1(null selection), R8.S2. Verify: `pnpm vitest run src/lib/secure-fs-posix.test.ts` fails as specified.
- [x] 2.2 **(WU-2, GREEN)** Create `src/lib/secure-fs-posix.ts`: `createLinuxAclAdapter(spawn?)` (`getfacl --absolute-names --numeric --omit-header -- <path>`, `LC_ALL=C`/`LANG=C`, bounded timeout, argv never a shell string, only base `user::`/`group::`/`other::` accepted — Algorithm D), `createMacosAclAdapter(spawn?)` (`/bin/ls -lde -- <path>`, `LC_ALL=C`, reject `+` flag + numbered ACEs — Algorithm E), `createPosixSecureFs(acl)` implementing the full `PlatformSecureFs` (ownership+no-group/other-write proof, `openDirNoFollow` capturing dev+ino, exclusive create, etc.), `selectSecureFs(platform?)` (linux→Linux adapter, darwin→macOS adapter, win32/other→`null`). Fail-closed everywhere; never degrade to mode-only, never strip an ACL. Verify: `pnpm vitest run src/lib/secure-fs-posix.test.ts`.

## Phase 3: WU-3 — transaction engine (RED → GREEN)

- [x] 3.1 **(WU-3, RED)** Create `src/lib/secure-fs-transaction.test.ts` (failing) driving `runTransaction` ONLY through a synchronous in-memory fake `PlatformSecureFs` (host-independent, no root): fresh-install segment creation at `0o700` one-at-a-time with post-create identity revalidation; capture + forced-only persistent backup (naming `<base>.javi-forge.bak.<ISO-ms>.<8hex>`, `O_CREAT|O_EXCL|O_WRONLY|O_NOFOLLOW` `0o600`, 8-candidate nonce retry, exact prior mode) present ONLY when `forceBackup`; routine upgrade/migrate leave `backups[]` empty; pre-first-rename full-gate re-prove (identity+ownership+ACL on every held ancestor — JD-007); asset-then-settings commit order with parent fsync; identity drift (incl. after `writeExclusive` — JD-B-005) aborts pre-commit; ACL refusal / exclusive-create `EEXIST` / fsync fault (via `writeExclusive` — JD-B-004) / rename fault; second-target failure → guarded reverse-order rollback restoring in-memory prior bytes via a transient rollback-temp; STOP-on-lost-proof / post-write hash drift emits manual-recovery guidance; empty identity-matched segment cleanup only; deterministic clock/nonce names. Covers R2.S3, R3.S1, R3.S2, R3.S3, R4.S1, R4.S2, R7.S2, R8.S1. Verify: `pnpm vitest run src/lib/secure-fs-transaction.test.ts` fails as specified.
- [x] 3.2 **(WU-3, GREEN)** Create `src/lib/secure-fs-transaction.ts`: define the `PlatformSecureFs` interface + `SecureIdentity`/`SecureDirHandle`/`CapturedFile`/`SecureRefusal`/`SecureResult`/`TransactionDeps`; implement platform-agnostic `runTransaction` (never spawns a process, never branches on `process.platform`) per Algorithm C — preflight parent-chain gate, one-segment `0o700` dir creation, capture (`captureFile` documented `O_NOFOLLOW|O_RDONLY` — JD-B-003) + forced-only backup + stage with post-create revalidation, pre-first-rename full-gate re-prove, asset-first/settings-second rename with parent fsync, guarded reverse-order rollback from in-memory `CapturedFile.bytes` via rollback-temp, STOP on lost-proof/hash-drift; backup/temp name builders taking injected `clock`+`nonce`. `writeExclusive` folds write+fsync so fsync is an injectable fault of that one method. Verify: `pnpm vitest run src/lib/secure-fs-transaction.test.ts`. STOP-FOR-SPLIT: if the commit exceeds 400 lines, split the fault-test file with its owning behavior into a follow-up commit, never defer safety tests.

## Phase 4: WU-4 — manager wiring (RED → GREEN)

- [x] 4.1 **(WU-4, RED)** Extend `src/lib/claude-hook-manager.test.ts` (failing) exercising the FULL state→action matrix through `_run(projectDir, mode, opts, { secureFs: fake, clock, nonce, manifest })` — always run, never platform-skipped: `absent`→install, `released-outdated`→upgrade, whole-file `exact-legacy`→migrate (`buildManagedContainer`), embedded `exact-legacy`→migrate (`planLegacyCohortExcision`, siblings preserved), `edited-managed`→refuse (no force), force-replace in place preserving siblings when matcher exact, force-replace when matcher edited with 0 siblings, force-REFUSE only when matcher edited with `siblingHandlers>0` (§324), `foreign|symlink|non-regular|malformed|partial-legacy`→refuse untouched, `managed-current`→byte+mtime zero-write no-op (transaction never entered), forced-only `backups[]` population, Windows refusal (`secureFs===null`→`windows-secure-object-unavailable`, zero mutation), and `ClaudeHookMutationResult.report` returned on success AND refusal. Plus a small real-adapter suite over a private symlink-free `mkdtemp` + `chmod 0700` ancestor tree asserting install/repair idempotency (re-run = zero write), skipped ONLY on incapable hosts. Covers R1.S1, R1.S3, R1.S5, R6.S1, R7.S1, R7.S2, R8.S2. Verify: `pnpm vitest run src/lib/claude-hook-manager.test.ts` fails as specified.
- [x] 4.2 **(WU-4, GREEN)** Wire `src/lib/claude-hook-manager.ts`: internal `_run(projectDir, mode, options, deps)` seam (Algorithm A) — classify both components (Slice-2 read layer), route terminal states (`absent` / whole-file `exact-legacy` → `buildManagedContainer`) vs embedded cohort → `planLegacyCohortExcision`, NEVER feed `undefined` to a merge planner; `resolveActions` (per-state plan-producer selection + §324 force matcher-exactness gate); `serializeSettings`/`applyPlan`/`readPackagedAssetBytes`; delegate irreversible I/O to `runTransaction`; `secureFs = deps.secureFs ?? selectSecureFs(platform)`, `null` → refuse `windows-secure-object-unavailable` zero-mutation; add `report: ClaudeHookDoctorReport` to `ClaudeHookMutationResult` and return a fresh `doctor()` snapshot on every path. Replace the two throwing stubs with public `installClaudePreToolUse(projectDir)` / `repairClaudePreToolUse(projectDir, options?)` (signatures UNCHANGED) building real deps (`selectSecureFs(process.platform)`, real clock, `randomBytes(4).toString("hex")`) and delegating to `_run`. Verify: `pnpm vitest run src/lib/claude-hook-manager.test.ts`.

## Phase 5: REFACTOR + boundary assertion

- [x] 5.1 **(REFACTOR)** Assert the module boundary (Security and Complexity Boundaries): `secure-fs-transaction.ts` never spawns a process and never branches on `process.platform`; the POSIX shell tool + ownership-bit interpretation live ONLY in `secure-fs-posix.ts`; `claude-hook-settings.ts` imports no `node:fs` and has no runtime `Manifest`/manager import (type-only if any); the manager opens no handle and renames nothing (it classifies/plans/serializes/delegates). No Ink/React in `src/lib/*`. Consolidate shared name builders/constants; confirm Slice-2 classifier/planner semantics untouched. Verify: `pnpm typecheck && pnpm typecheck:test && pnpm lint`.

## Phase 6: Verification and gate closure

- [x] 6.1 Run `pnpm validate` (typecheck + typecheck:test + lint + test) — green. (G.2, G.8)
- [x] 6.2 Run `pnpm test:coverage` — lines ≥ 85, branches ≥ 80, thresholds unchanged. (G.8)
- [x] 6.3 Run `pnpm package:check` — green; confirm `manifest.json` (`installerHelpers.windowsSecureObject` still `null`) and `package.json` unchanged, no production dependency added. (G.7, G.8)
- [x] 6.4 Confirm write/size/skip gates: no test on a refuse/no-op path creates/renames/backs up any file; `managed-current` re-run asserts byte+mtime unchanged and no backups; core matrix runs through the fake seam (never platform-skipped), adapter-refusal tests never skip, real-ACL/real-adapter fixtures skip only on incapable hosts; `git diff --stat` keeps each WU commit < 400 and the PR under the pre-approved `size:exception`. (G.1, G.3, G.6, G.7)

## Requirement-to-Task Traceability

| Requirement | Scenario → task mapping | Coverage |
|---|---|---|
| R1 Install/repair follow the state→action matrix per component | S1(absent→install)→4.1,4.2; S2(released-outdated/exact-legacy)→1.1,1.2,4.1,4.2; S3(managed-current zero-write no-op)→4.1,4.2; S4(edited-managed refuse/force+backup)→1.1,1.2,4.1,4.2; S5(unsafe states refuse untouched)→4.1,4.2 | Complete |
| R2 A proven private parent chain gates every mutation | S1(group/world-writable refuse)→2.1,2.2,3.1,3.2; S2(extended/inconclusive ACL refuse)→2.1,2.2,3.1,3.2; S3(identity drift aborts)→3.1,3.2 | Complete |
| R3 Writes are atomic with same-directory exclusive backups | S1(fresh install creates parents safely then stages)→3.1,3.2; S2(backup captures exact prior bytes restrictively)→3.1,3.2; S3(commit atomic and asset-first)→3.1,3.2 | Complete |
| R4 Rollback is guarded and stops on lost proof | S1(guarded rollback restores earlier target)→3.1,3.2; S2(lost proof stops auto-cleanup)→3.1,3.2 | Complete |
| R5 Settings merges preserve unrelated content structurally | S1(unrelated settings survive a managed mutation)→1.1,1.2,4.2 | Complete |
| R6 Windows mutation refused until Slice 3b | S1(Windows install refuses without mutation)→2.1,2.2,4.1,4.2 | Complete |
| R7 The mutation result is reconciled and refusals never mutate | S1(successful mutation returns reconciled report)→4.1,4.2; S2(refusal produces no filesystem effect)→3.1,3.2,4.1,4.2 | Complete |
| R8 The transaction is host-independently testable | S1(injected faults exercise every failure path)→3.1,3.2; S2(adapter-refusal always run; core matrix never platform-skips)→2.1,2.2,4.1 | Complete |

### Traceability Gap Report

- Requirements mapped: **8/8**.
- Scenarios mapped: **19/19**.
- Unmapped requirements/scenarios: **none**.
- Work units: **4** (WU-1 settings helpers, WU-2 POSIX adapters, WU-3 transaction engine, WU-4 manager wiring); ordered WU-1 → WU-2 → WU-3 → WU-4, each commit < 400 lines inside one `size:exception` PR.
