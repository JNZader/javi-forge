# Review Ledger — ci-engine-unification (design phase, judgment-day)

Round 1: two blind judges (A: REJECT — 1 BLOCKER, 2 CRITICAL; B: APPROVE-WITH-FIXES — 3 CRITICAL). Convergent findings merged below; fix pass applied 2026-08-08. Round 2: scoped re-judge (judge A) — **APPROVED**, 16/16 verified resolved against code (clover.xml coverage figures, ci.ts:13/:957 orphan import, ci.ts:880-886 security-phase skip conditions, verify-package-contents.mjs:122-126 prefix-check reasoning, CRLF regex behavior and slice-1 assertion invariance re-derived independently).

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JD-001 (JDA-001/JDB-003) | judgment-day | design.md:178→209,238-247 | BLOCKER | fixed | Planned test edits violated spec MUST ci-execution:110 ("ADDED tests only"); slice-1 characterization assertion was flipped by slice 2. Fix: ensureImage mock returns getImageName(runner.stack) → assertion invariant; grep tests untouched, byte-equality ADDED. |
| JD-002 (JDA-002/JDB-001) | judgment-day | design.md:34,91→39-43,111-122,232 | CRITICAL | fixed | .bak write path unguarded vs symlink (same attack ci.ts:1127-1146 defends for the hook). Fix: lstat every backup target, refuse symlink/non-regular even with --force, O_EXCL create with bounded retry. |
| JD-003 (JDA-003/JDB-011) | judgment-day | design.md:51-68→81-91 | CRITICAL | fixed | historical[] had no forward maintenance; generator mined TS constants deleted by slice 3 → first v1→v2 bump bricks fleet ci init (the R2 cliff). Fix: binding append-on-change rule sourced from git show prev:assets/hooks/*, guarded by test; v1 assertion sha256===historical[v0]. |
| JD-004 (JDA-007/JDB-002) | judgment-day | design.md:34,172→45,43,233 | CRITICAL | fixed | Spec MUST ci-hook-install:86 (backup fails ⇒ no overwrite) had no design element or test row. Fix: abort semantics + matrix row; backup copies original bytes, preserves mode. |
| JD-005 (JDA-005/JDB-009) | judgment-day | design.md:11-13→12-15 | WARNING | fixed | D1 precondition false for auto + --no-docker (prologue ensureImage inside if(!noDocker), ci.ts:536-573). Fix: precondition auto && !noDocker; skip nested inside !noDocker guard. |
| JD-006 (JDB-005) | judgment-day | design.md:90/66→111-122 | WARNING | fixed | Data flow said write on managed-current, D6 said zero writes. Fix: no-write idempotence, diagram branch added. |
| JD-007 (JDB-006) | judgment-day | design.md:182→251 | WARNING | superseded | Coverage evidence fabricated (~9k/88%). SUPERSEDED — the round-1 "measured" figures (3256/3001 = 92.2%) came from a STALE `coverage/clover.xml` and are also wrong. The real baseline was measured during the slice-1 apply: see the Apply section below (JDB2-003) and the design's Coverage Guard table. |
| JD-008 (JDB-007) | judgment-day | design.md:73→95 | WARNING | fixed | Packaging assertion covered 1 of 3 assets; prefix check passes on any single match. Fix: all three assets + manifest in REQUIRED_FILES. |
| JD-009 (JDA-008/JDB-012) | judgment-day | design.md:139→172-180,55 | WARNING | fixed | InstallHooksResult could not express "reported as upgraded" spec scenarios. Fix: upgraded[] + states[]; dispatch prints upgrades distinctly. |
| JD-010 (JDA-006) | judgment-day | design.md:18-25→21,27-28,212 | WARNING | fixed | doneLabel on shared descriptor would regress suffixed labels ("Tests [api] passed"). Fix: scoped to bare mode; suffixed test row added. |
| JD-011 (JDB-014) | judgment-day | design.md File Changes→129 | SUGGESTION | fixed | Deleting ci.ts:957 orphans getImageName import (ci.ts:13); biome fails. Fix: import removal added to File Changes. |
| JD-012 (JDA-009) | judgment-day | design.md:58-64→71,166-167,231 | WARNING | fixed | No HOOK_STATE for non-regular file; EISDIR unmodeled (test ci.test.ts:323-334 creates directory). Fix: NOT_A_FILE state + lstat step 0 + matrix row. |
| JD-013 (JDB-004) | judgment-day | design.md:61-64/173→76,229,234 | WARNING | fixed | CRLF-marked hook fails $-anchored marker regex → classifies foreign, but test row asserted managed-edited. Fix: row aligned with algorithm. |
| JD-014 (JDB-013) | judgment-day | design.md:62→74 | SUGGESTION | fixed | Marker name never bound to slot (pre-commit marker in pre-push slot silently overwritten). Fix: name !== hookName → foreign. |
| JD-015 (JDB-015) | judgment-day | design.md:102→247 | SUGGESTION | fixed | Stryker mutates src/constants.ts; HOOK_ASSETS_DIR string mutants unaddressed. Fix: testing-strategy line, classification tests use exported constant. |
| JD-016 (JDA-004) | judgment-day | specs/ci-execution/spec.md:56-57→55-60 | WARNING | fixed | Preserved-order MUST omitted per-runner security phase (ci.ts:880-886). Fix: enumeration completed with skip conditions + MUST-NOT-reorder clause. |

## Round 2 — new findings on fix-touched lines (severity floor: info, non-blocking)

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JDA-R2-001 | judgment-day | design.md:89 | WARNING | info | Forward-maintenance guard "skipped when git history unavailable" — actions/checkout fetch-depth:1 makes it skip exactly in CI. Fold into slice 3 at apply: pure-file guard (historical[] strictly grows when sha256 changes) or pin fetch-depth:0 + fail-on-skip. |
| JDA-R2-002 | judgment-day | design.md:42-43 | SUGGESTION | info | Backup step 3 offers bare fs.copyFile which is neither exclusive nor no-follow; name the safe forms (COPYFILE_EXCL or open "wx" + Buffer write). |
| JDA-R2-003 | judgment-day | specs/ci-execution/spec.md:56-59 | SUGGESTION | info | New security-phase ordering MUST has no scenario and no Testing Strategy row; add configured-runner ordering assertion in slice 2. |

## Apply slice 1 — judgment-day

Two blind judges on the slice-1 apply diff (characterization net + hook-exec integration test + artifact updates): both returned APPROVE-WITH-FIXES. Fix pass applied 2026-08-08 on `feat/ci-unification-s1-characterization`.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JDA2-001 | judgment-day | src/commands/ci.test.ts:1089-1147 | CRITICAL | fixed | Order assertions used `[...new Set(steps.map(s => s.id))]`, blind to a DUPLICATE `docker-image` emission — the exact slice-2 regression the net exists to catch. Fix: raw-stream test pins `docker-image` at exactly 2 emissions (running→done from a single build), full stream at 14 events, plus `ensureImage` called exactly once. Set-based membership/order assertions kept. |
| JDB2-003 | judgment-day | design.md:251 | CRITICAL | superseded | Coverage block quoted 3256/3001 = 92.2% "far above the 85/80 floors" — from a stale clover.xml. Round-2 fix replaced it with measured absolutes (main 87.65%/77.97%, slice 1 88.34%/78.88%) and an absolute slice-2 floor. SUPERSEDED by JDB3-001: absolute floors are the defect, not the numbers — see that row. `pnpm test:coverage` FAILS the configured 80% branch threshold on main (pre-existing, COV-1); `pnpm validate` does not run coverage. |
| JDA2-002 / JDB2-002 | judgment-day | src/commands/ci.test.ts:1226-1242 | WARNING | fixed | The `security:*` absence assertion ran under `noSecurity: true`, where ci.ts:884 skips the phase unconditionally → vacuous. Fix: full-mode variant with `noSecurity: false`; `semgrep`/`ghagga` probes stubbed unavailable at the `../lib/exec.js` module boundary for determinism. Structural `securityCmds).toEqual([])` assertion kept. |
| JDA2-003 / JDB2-001 | judgment-day | src/commands/ci.test.ts | WARNING | fixed | Net pinned ids only — labels and statuses were unguarded, leaving slice-2 task 2.1 to author its naming table blind. Fix: `(id, label, status)` tuple stream for auto+Docker measured and pinned (14 events, including the `context-refresh` skip). |
| JDB2-005 | judgment-day | src/commands/ci.test.ts | WARNING | fixed | No characterization of the `--no-docker` gate. Fix: auto + `noDocker: true` asserts zero `docker-image`/`docker-check` steps, `ensureImage` never called, `runInContainer` never called — pins the D1 `!noDocker` guard nesting for slice-2 task 2.6. |
| JDB2-006 | judgment-day | tasks.md:46 | WARNING | fixed | Task 1.10 said hooks are executed with `sh <hook>`; the shipped test executes them directly via shebang. Fix: task text updated with the deviation rationale (hooks are `#!/bin/bash` — ci.ts:1030/:1047/:1069 — and use bash arrays; dash cannot parse them, and direct execution is what git does). |
| JDB2-008 | judgment-day | tasks.md:84 | WARNING | fixed | The file-wide `ensureImage` mock is production-faithful only without `buildContext` (ignores `imageTag`). Fix: note on task 2.11 requiring the mock be extended to `imageTag ?? getImageName(stack)` BEFORE any build-context test row. |
| JDA2-004 / JDB2-004 | judgment-day | specs/ci-execution/spec.md:121-130 | WARNING | fixed | "Coverage floors preserved" asserted 85/80 floors that main does not meet, and pointed at `openspec/config.yaml` (thresholds live in `vitest.config.ts`). Fix: MUST restated as no-regression vs the slice-1 measured baseline + no threshold lowering; the unmet 80% branch floor recorded as pre-existing and tracked in docs/BACKLOG.md. Superseded by JDB3-001 — that absolute baseline is now a same-run delta. |
| JDA2-005 | judgment-day | src/commands/ci.test.ts:~1075 | SUGGESTION | info | Non-node characterization hard-codes `javi-forge-ci-go` instead of deriving via `getImageName("go")`. Not fixed: the literal is the point (it would survive a mock that returns a sentinel). |
| JDA2-006 | judgment-day | src/__integration__/ci-hooks-exec.integration.test.ts | SUGGESTION | info | Direct-shebang execution depends on `installCIHooks` setting the exec bit; on a filesystem mounted `noexec` the suite fails for an environmental reason. Accepted — same dependency git itself has. |
| JDB2-007 | judgment-day | src/__integration__/ci-hooks-exec.integration.test.ts | SUGGESTION | info | `runHook` collapses a spawn error into a synthetic exit code, so a missing interpreter is indistinguishable from a hook rejection. Not blocking: every assertion also pins stdout/stderr content. |
| JDB2-009 | judgment-day | src/commands/ci.test.ts | SUGGESTION | info | Image-threading assertions cover the auto path only; the configured/build-context threading residual is slice-2 territory (task 2.11 note). |
| JDB2-010 | judgment-day | src/__integration__/ci-auto-docker.integration.test.ts | SUGGESTION | info | The Docker-gated test accepts any locally present image, including a stale one — it verifies wiring, not image freshness. Accepted, recorded as part of the residual R1 gap. |

Verified-correct load-bearing claims (both judges, no finding): ensureImage byte-identity (docker.ts:186/221/255), no duplicate docker-image step, FORGE_ROOT ESM resolution, hash round-trip byte-exactness, Stryker does not mutate ci.ts, compat anchor ci.test.ts:767-787. NOTE: the round-2 entry "coverage floors honored" is RETRACTED — the configured 80% branch floor is NOT met (COV-1), and absolute floors were retired entirely by JDB3-001 in favour of a same-run delta gate.

## Round 3 — scoped re-judge on the round-2 fix diff

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JDB3-001 | judgment-day | design.md:249-265, specs/ci-execution/spec.md:121-136, tasks.md:87 | CRITICAL | fixed | Third iteration of the same defect class: the coverage gate was still expressed as ABSOLUTE percentages (`>= 88.34` lines / `>= 78.88` branches), which are (a) stale one commit after being written — head `1f5c69b` already measures 3300/3725 = 88.59% lines and 1904/2411 = 78.97% branches, above the "floor" written for `12d9b4d` — and (b) environment-dependent, because the Docker-gated integration suites run on a developer box and `skipIf` out in CI, so the same commit reports different coverage on different machines. Fix: the FORM of the gate is restated everywhere as a SAME-RUN DELTA — run `npx vitest run --coverage` twice on the same machine in the same session (merge-base tree, then slice head, identical command and environment) and require `head >= base` on both metrics, tolerance 0. Absolute percentages are demoted to informative context carrying their commit and environment. Applied to design.md "Coverage Guard (R4)", spec `ci-execution` → "Coverage must not regress" (requirement + scenario), tasks.md 1.13, 2.13, 3.22. Head figures re-measured independently by the fix agent (`npx vitest run --coverage`, 76 files / 1277 passed / 4 skipped, Docker present): identical to the judge's. |
| JDB3-002 | judgment-day | tasks.md:46, review-ledger.md JDB2-006 | WARNING | fixed | Both cited the generated hooks as `#!/usr/bin/env bash`; they declare `#!/bin/bash` (ci.ts:1030, :1047, :1069). The deviation rationale (dash cannot parse bash arrays; direct execution is what git does) is unaffected. Fix: both citations corrected with the ci.ts line anchors. |
| JDB3-003 | judgment-day | tasks.md:49,60-66,120; proposal.md:71; exploration.md:185; review-ledger.md:52; docs/BACKLOG.md:87-94 | WARNING | fixed | Stale `85/80` and `92.2%` leftovers survived the round-2 fix in files the fix did not touch. Fix: task 1.13 restated around the delta gate; the "UNREACHABLE … blocker" paragraph replaced with the informative-readings section pointing at COV-1 (the unmet configured floor is orthogonal — no slice is blocked on closing it); task 3.22 moved to the delta form; proposal R4 and exploration R4 repointed at the delta gate (exploration's `openspec/config.yaml` mis-citation corrected to `vitest.config.ts`); the ledger's "coverage floors honored" claim retracted; BACKLOG COV-1 figures relabeled with commit + Docker-availability context. |
| JDB3-004 | judgment-day | review-ledger.md JDA2-001 | SUGGESTION | fixed | Row cited `src/commands/ci.test.ts:1000-1056`; the duplicate-sensitive raw-stream and label/status tests it describes live at :1089-1147. Fix: location corrected. |
| JDB3-005 | judgment-day | docs/BACKLOG.md COV-2 | SUGGESTION | info | COV-2 proposes wiring `test:coverage` into `validate`/CI only after COV-1 lands. Worth revisiting once the delta gate exists: a delta check is wireable into CI BEFORE COV-1 closes, since it never asserts an absolute floor. Not actioned here — out of scope for this slice. |

## Apply slice 1 — round 4 (final scoped verification, judge B)

Convergence budget exhausted after round-2 fix. Verdict: JDB3-001/002/003 verified fixed; ONE item remained open and was closed by the orchestrator as mechanical propagation of the already-approved delta-gate decision (single-line edits, no new fix cycle).

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JDB4-001 | judgment-day | proposal.md:89 (+ :56) | CRITICAL | fixed | Success Criteria checkbox still gated on the retired absolute floor (>= 85/80) — unmet on main, unreachable per COV-1, contradicting every other artifact. Fixed by orchestrator post-budget: checkbox restated as the same-run delta; the :56 constraint line repointed (declared in openspec/config.yaml:19-21, enforced by vitest.config.ts:19). |
| JDB4-002 | judgment-day | tasks.md:85 | WARNING | info | Phase 2 header cites spec requirement "Coverage floors preserved", renamed to "Coverage must not regress" by round-2 fix. Dangling reference — queued for the slice-2 apply prompt. |
| JDB4-003 | judgment-day | tasks.md:100,:133 | SUGGESTION | info | Dropping `pnpm test:coverage` from command lists was based on a void rationale (the substitute `npx vitest run --coverage` is the identical command, package.json:17, red-gates identically). Harmless outcome; restore the named script as an explicitly non-gating invocation during slice 2. |
| JDB4-004 | judgment-day | proposal.md:56 (fixed above), exploration.md:145 | SUGGESTION | info | Residual `openspec/config.yaml` threshold citations — declares vs enforces distinction now stated at proposal.md:56; exploration.md:145 left as historical record. |

## Apply slice 2 — judgment-day (executor collapse)

Two blind judges, both **APPROVE** — zero BLOCKER/CRITICAL candidates, no fix round. Behavior preservation verified line-by-line by both against the deleted `runLegacySteps`; invariant throw proven unreachable on all legitimate paths; naming keyed strictly on `resolved.source`; slice-1 net byte-intact (only the pre-authorized JDB2-008 mock extension). All findings below are severity-floor `info`.

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JDA5-001 / JDB5-003 (convergent) | judgment-day | tasks.md 2.13/3.22, design Coverage Guard, spec ci-execution | WARNING | fixed | Gate metrology: unit never stated (raw covered-line counts DROP on a deletion refactor — 3300→3290 — gate only passes as percentages) AND ±1-branch inter-run jitter makes "tolerance 0" empirically false. Fixed post-round: gate restated as percentages with the observed ±1-branch equality band. |
| JDB5-001 | judgment-day | ci.ts:897-904 | WARNING | info | Bare-mode FAILURE labels ("Tests failed") pinned by zero tests repo-wide; rewrite was correct by inspection but unguarded. → slice-3 test pass. |
| JDA5-004 | judgment-day | ci.test.ts:1465-1470 | SUGGESTION | info | Config-path ordering test uses the Set pattern ruled insufficient in JDA2-001 — blind to duplicate emissions on the config path. → slice-3 test pass. |
| JDB5-002 / JDA5-003 (convergent) | judgment-day | ci.ts:878/:632 + ui/CI.tsx:55 | WARNING | info | Latent bare-mode `security`/`setup` id collision with top-level step ids; TUI dedup would silently overwrite. Unreachable today (auto cmds frozen empty, pinned by slice-1 tests). Correctly parked → gates-v2 prompt. |
| JDB5-004 | judgment-day | ci.test.ts:34-36 | SUGGESTION | info | Extended mock is MORE permissive than production (honors imageTag without buildContext; drops the no-Dockerfile fail-closed throw). No current test wrong. → tighten during slice 3 if touched. |
| JDA5-005 | judgment-day | ci.test.ts beforeEach | SUGGESTION | info | mockClear() does not clear queued mock*Once values — latent cross-test poisoning. → consider mockReset/restoreMocks in slice 3. |
| JDA5-006 | judgment-day | ci.test.ts:1510-1513 | SUGGESTION | info | Exact-object assertion on internal ensureImage call shape — implementation coupling, redundant next to the observable assertions. |
| JDB5-005 | judgment-day | ci.test.ts:1435-1455 | SUGGESTION | info | Invariant-throw test reaches the branch via a state production cannot produce — coverage by construction, documented as such. |

## Apply slice 3a — judgment-day (hook asset extraction)

Two blind judges, both **APPROVE** — zero BLOCKER/CRITICAL. Byte-fidelity verified independently by BOTH via different methods (byte accounting + sha256 recomputation); both REFUTED the orchestrator brief's escape premise (ci.ts:1062 has `\\\\b` → renders `\\b`, asset correct). Census reproduced under --full-history (12 revisions, 1 variant/hook, firstCommit 5587b3b — the render-invariant biome commit 248e1b2 verified). Convergent info findings hardened post-round on this branch:

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JDA6-001 / JDB6-001 (convergent) | judgment-day | hook-assets.test.ts snapshot | WARNING | fixed | Guard compared a COUNT — the documented snapshot-update path let a dev rewrite historical[0] and go green (fleet-brick attack traced independently by both judges). Hardened: RELEASED_SNAPSHOT holds the full hash LIST with an append-only PREFIX assertion + explicit attack-case test (21st test). |
| JDA6-004 / JDB6-002 (convergent) | judgment-day | repo root | WARNING | fixed | No .gitattributes — core.autocrlf=true checkout would CRLF the hashed assets. Added: `assets/hooks/** text eol=lf`. |
| JDA6-006 / JDB6-003 (convergent) | judgment-day | design.md D6:89 | SUGGESTION | fixed | Design still described the rejected git-show/skip-when-unavailable guard; next 3b reader would regress it. Rewritten to the implemented pure-file append-only mechanism. |
| JDA6-002 / JDB6-004 (convergent) | judgment-day | hook-assets.test.ts (absent guard) | WARNING | info | Live byte-equivalence assertion (installCIHooks output vs assets) is possible ONLY while the inline constants exist — after 3.20 the claim is unfalsifiable forever. → MUST land as the FIRST task of 3b, before the constants are deleted. |
| JDA6-003 | judgment-day | build-hook-history.mjs:147 | SUGGESTION | info | rev-list --all determinism depends on local refs (census narrative "259 commits" now reads 276 — ref growth); load-bearing figures reproduce. One-shot bootstrap, accepted. |
| JDA6-005 | judgment-day | build-hook-history.mjs exports | SUGGESTION | info | renderTemplateLiteral/extractHooks exported but never imported by tests — renderer has zero direct coverage; outside lint scope. Accepted for a one-shot script. |
| JDB6-005 | judgment-day | tasks.md coverage label | SUGGESTION | info | Readings recorded against c6ddeaa while branch head was 3dae961 (immaterial: file outside coverage/lint/tsconfig scope; numbers reproduce). Label hygiene only. |

## Apply slice 3b — inherited items closed at apply time

No new judgment-day round yet (the gate runs after this apply). Items carried
into this slice by earlier rounds, and their disposition:

| id | source | status | evidence |
|---|---|---|---|
| JDA6-002 / JDB6-004 | slice 3a | fixed | The live byte-equivalence assertion landed as the FIRST commit of 3b (`35eb5d0`), while the inline constants were still the write source — the only window in which it is falsifiable. Mutation probe: appending one byte to `assets/hooks/pre-commit` failed it. It then passed UNCHANGED across `b8f3800` (write source switched to the assets), which is the continuity proof that no byte moved. |
| JD-008 | design round 1 | fixed | All four asset paths are in `REQUIRED_FILES` by name. Probe: deleting `assets/hooks/commit-msg` from the packed manifest fails the check with `missing required file`, while `REQUIRED_PREFIXES` alone still passes on the three survivors. |
| JD-004 | design round 1 | fixed | Backup-throws row: `fs.copyFile` stubbed to reject with `ENOSPC` → the hook is byte-unchanged, no `.bak` exists, `backups` is empty, the error names ENOSPC, and both sibling hooks still install. |
| JD-002 / JDA-R2-002 | design round 1/2 | fixed | Every backup candidate is `lstat`ed; a symlinked or directory `.bak` refuses the hook even WITH `--force`, leaving the pointed-at file intact. Creation uses `copyFile` + `COPYFILE_EXCL` (never bare `copyFile`, never `existsSync` + `writeFile`), copies original BYTES (asserted with a deliberately non-UTF8 hook) and restores the mode. |
| JD-014 | design round 1 | fixed | Marker name bound to the slot: a valid `pre-push` marker sitting in the `pre-commit` slot classifies `foreign` and is refused. |
| JDB5-001 | slice 2 | fixed | Bare and suffixed FAILURE labels now pinned by one error-driving row each ("Tests failed" / "Test [api] failed"), each asserting the other spelling is absent. |
| JDA5-004 | slice 2 | fixed | Duplicate-sensitive RAW-stream assertion added alongside the Set-based config-path ordering assertions (each phase exactly 2 emissions, `ensureImage` exactly once). |
| JDA5-005 | slice 2 | fixed | `mockClear` → `mockReset` in both `beforeEach` blocks. Not theoretical: the new failure-path rows install a `mockImplementation` and immediately poisoned three sibling tests under `mockClear`. No pre-existing assertion was edited — only mock setup. |
| JDB5-004 | slice 2 | fixed | `ensureImage` mock tightened to honor `imageTag` ONLY with `buildContext`, matching docker.ts:173-174/186. Verified it cannot flip any outcome: the single test passing `imageTag` also passes `buildContext`. |

New deviation recorded for the judges: one pre-existing integration assertion
("overwrites existing hooks") was REPLACED because the `ci-hook-install` spec in
this same change reverses that behavior. See the 3b deviation note in tasks.md.

## Apply slice 3b — judgment-day

Two blind judges (A: JDA7-001..011, B: JDB7-001..009). ONE CRITICAL, convergent.
Fix pass applied 2026-08-08 on `feat/ci-unification-s3b-classify`, TDD (every
behavioral row landed red first: 8 failing tests, then green).

| id | lens | location | severity | status | evidence |
|---|---|---|---|---|---|
| JD7-001 (JDA7-001/JDB7-001, convergent) + JDA7-002 | judgment-day | ci.ts:1409-1417, :1392-1400 | CRITICAL | fixed | `writeFile(..., {mode: 0o755})` applies the mode ONLY on CREATION. Overwriting an existing 0644 hook (forced foreign, legacy-v0 upgrade, managed-outdated upgrade) left it non-executable and git skips a non-executable hook SILENTLY — the CI gate evaporates with no error. Fix: unconditional `fs.chmod(hookPath, 0o755)` after every write, plus `repairHookMode()` on the `managed-current` no-op path (chmod only when `(mode & 0o777) !== 0o755`). The repair writes no bytes and `chmod` touches `ctime`, not `mtime`, so the zero-write idempotence test's bytes+mtimeMs assertions still hold — asserted explicitly by the new managed-current mode-repair test. Three new tests: forced overwrite of a 0644 foreign hook (hook 0755, backup keeps 0644), legacy-v0 upgrade of a 0644 historical body, managed-current 0644 → repaired with bytes and mtime unchanged. |
| JD7-002 (JDA7-007/JDB7-002/JDB7-003, convergent) | judgment-day | ci.ts:1281-1318, :1355-1377, :1389 | CRITICAL | fixed | `loadHookManifest()` and `fs.ensureDir()` were unguarded: a missing/corrupt shipped manifest rejected straight out of `installCIHooks` into `handleCi`, which awaits it with no catch → unhandled rejection instead of an operator-actionable message. `manifest[name]` was also unguarded → `TypeError: Cannot read properties of undefined (reading 'version')` for a manifest missing one hook key. Fix: both boundary calls wrapped, returning a named `errors[]` entry (path + reason code + remedy "reinstall javi-forge … or repack it from source"); `assertHookManifestEntry()` validates `version:number / sha256:string / historical:array` per hook and throws a NAMED per-hook error inside the isolation try, so a defect in one entry does not block the siblings. Three tests: unreadable manifest, manifest missing `commit-msg`, manifest with `historical` not an array. |
| JD7-003 (JDA7-010/JDB7-004, convergent) | judgment-day | ci.ts:1190-1210 | CRITICAL | fixed | `describeBackupTarget` probed with `fs.pathExists`, which FOLLOWS symlinks: a symlink at `<hook>.bak` pointing at an existing file read as "taken" (message promises `.bak.<timestamp>`) and a DANGLING one read as "free" (message promises `.bak`). Both are lies — `backupHook` refuses a non-regular backup target even WITH `--force`, so the promised backup never happens and the user is told to run a command that will refuse. Fix: renamed to `describeBackupPlan`, probes with `lstat`, and when the `.bak` path is a symlink or non-regular file the message says force will REFUSE the hook until that path is removed instead of promising a backup. Two tests (symlink, directory) assert the obstruction is named and that "saved as" is absent. |
| JD7-004 (JDB7-005) | judgment-day | ci.test.ts:461 | WARNING | fixed | `(stat.mode & 0o777) === 0o755` was umask-fragile: `writeFile({mode})` is masked at creation. Verified EMPIRICALLY rather than by inspection — the assertion goes RED under `umask 077` on the pre-fix tree and GREEN on the post-fix tree, because `chmod` ignores the umask. Strict form KEPT (JD7-001's unconditional chmod makes it umask-independent); rationale recorded in a comment at the assertion. |
| JD7-005 (JDA7-006) | judgment-day | ci-hooks.test.ts force describe | WARNING | fixed | Force matrix had no row for the two states where force must change nothing. Added: `managed-current + --force` → NO-OP (installed/upgraded/backups all empty, bytes and mtime unchanged, no `.bak` — the guard precedes the force branch) and `managed-outdated + --force` → upgrade with no backup, identical to the no-force path. Plus a COPYFILE_EXCL guard: `fs.copyFile` is spied (call-through) and the third argument on the `.bak` call asserted `constants.COPYFILE_EXCL`, so deleting the flag now goes red instead of silently degrading the atomic create. |
| JD7-006 (JDA7-009) | judgment-day | ci-hooks.test.ts classification describe | WARNING | fixed | The "one byte of drift" row used a synthetic body, so it proved nothing about the SHIPPED matcher. Replaced with the real asset: `readAsset("pre-commit")` classifies `legacy-v0` (control) and the same bytes plus ONE trailing space classify `foreign` — the byte-exact, non-fuzzy matcher asserted against the released artifact and its real manifest entry. |
| JD7-007 (JDB7-006) | judgment-day | specs/ci-hook-install/spec.md | WARNING | fixed | Spec vocabulary predated the implementation: it said `managed:vN` / `managed:v0` / `managed-but-edited` where the shipped `states[]` emits `managed-current`, `managed-outdated`, `managed-edited`, `legacy-v0`, `symlink`, `not-a-file`; and "`managed:vN` MUST be upgraded" contradicted the shipped (and correct) managed-current NO-OP. Fix: "Classify before write" restated over the real vocabulary with the current/outdated distinction spelled out, the zero-write carve-out, the exec-bit-on-every-write-path MUST and the symlink/not-a-file refusal; new requirements for marker-vs-slot binding, the honest refusal message, backup-target protection + COPYFILE_EXCL + "no backup for states written anyway", and named manifest errors. Shipped-behavior citations in design.md (D5 heading, :5, :55) and tasks.md:191 realigned to `legacy-v0`; proposal.md and exploration.md left as pre-decision historical record per the JDB4-004 convention. |
| JDA7-011 | judgment-day | ci-local/docker/python.Dockerfile | WARNING | fixed | An untracked `ci-local/docker/python.Dockerfile` sat in the tree; `ci-local/` is in `package.json` `files`, and `npm pack` includes untracked-but-unignored files, so a locally packed tarball would ship a file that exists on no other machine — tarball contents stop being a function of the commit. Moved OUT of the repo (parked in the session scratchpad); `git status` is clean. |
| JDA7-003 / JDB7-008 (convergent) | judgment-day | ci.ts classify → write | WARNING | info | TOCTOU between `classifyHookPath`'s `lstat` and the `writeFile`: a symlink planted in that window is written through. Assessment: real but design-level — closing it needs `open(O_NOFOLLOW\|O_TRUNC)` + `fchmod` on the descriptor, not a patch. `.git/hooks` is already writable by anyone who could win the race. Parked with the same disposition as the backup-path window. |
| JDA7-004 | judgment-day | backupHook | WARNING | info | A HARDLINK at the backup path is neither a symlink nor a non-regular file, so it passes the guard and `COPYFILE_EXCL` fails with EEXIST → falls through to the timestamped name. Assessment: theoretical, and the fall-through is the safe outcome. No fix. |
| JDA7-005 | judgment-day | repairHookMode / post-write chmod | WARNING | info | `fs.chmod` follows symlinks. Assessment: reachable only through the JDA7-003 window (the path was `lstat`ed as a regular file moments earlier), and `fchmod` on the write descriptor is the same design-level fix. Parked with JDA7-003. |
| JDA7-008 / JDB7-009 (convergent) | judgment-day | diff noise | SUGGESTION | info | Formatter-driven reflow inflated the diff around untouched lines. Accepted — the repo formats on write. |
| JDB7-007 | judgment-day | ci-hooks.test.ts ENOSPC row | SUGGESTION | info | The ENOSPC mock sets `code` as an own enumerable property; a production `SystemError` carries it the same way, so `errorCode()` reads it identically. Noted, no change. |
| JDB7-006b | judgment-day | package.json:19 `test:hooks` | SUGGESTION | info | `pnpm test:hooks` runs `ci-local/hooks/commit-msg.test.sh` — it exercises the ci-local variant, NOT the shipped `assets/hooks/*` this slice installs. Not misleading enough to rename mid-slice; recorded so nobody reads a green `test:hooks` as coverage of the shipped assets. |
