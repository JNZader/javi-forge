# Tasks

- [x] CLI `preparation execute` wired to existing executor, dest-exists refuse before consume
- [x] Tests: dest-exists no consume; cwd mismatch no worker; diagnostics still inert
- [x] Help + runbook: execute exists; still no dest override / model / deploy
- [x] Focused vitest + typecheck; no homedir writes
