# Review Ledger — Judgment Day (design) · skillguard-runtime-gate

- **Phase reviewed**: sdd-design (design.md) against proposal + spec + real code
- **Round**: 1
- **Judges**: jd-judge-a, jd-judge-b (blind, parallel, identical criteria)
- **Target**: `openspec/changes/skillguard-runtime-gate/design.md`
- **Store**: hybrid (openspec + engram topic `sdd/skillguard-runtime-gate/review-ledger`)

## Findings

| id | lens | location | severity | status | assessment | evidence |
|----|------|----------|----------|--------|------------|----------|
| JD-001 | judgment-day | `src/lib/agent-skills.ts:140-148` (via `src/commands/plugin.ts:288`) | CRITICAL | fixed | — | Fail-open install entrypoint absent from design: `importAgentSkillsPackage` copies sourceDir into PLUGINS_DIR with NO scan (fs.copy agent-skills.ts:148, .installed.json :164); reachable via `javi-forge plugin import` (commands/plugin.ts:288, VALID_PLUGIN_ACTIONS simple-renderers.tsx:62-72). Alternate call path defeats the whole gate — a block-scanning skill refused by `plugin add` installs unconditionally via `plugin import`. Both judges independently confirmed. FIXED in design.md (D8 gate placement, D2/D5 scope, Data Flow, File Changes, Interfaces, Testing 13/14). |
| JD-002 | judgment-day | `src/lib/skill-scanner.ts:531-534` × design.md:12 | WARNING | fixed | real | Plugin gate reuses `scanSkillsDirectory(tmpDir)` which treats `PLUGIN.md` as a skill. Conventional Claude-plugin READMEs document `~/.aws`/`~/.ssh`, curl examples, "bypass" wording → false-positive `block` on docs → permanent hard lockout (--force bypasses only `unscannable`, never `block`) with no escape hatch (staging removed before user can edit). Both judges independently flagged. FIXED in design.md (D1: declared-skills scan for plugin add + import gate; risk note; Testing 14). |
| JD-003 | judgment-day | `src/lib/skill-scanner.ts:511-540` × design.md:69 | WARNING | fixed | real | Walk uses `fs.stat` (follows symlinks) with no lstat/visited-set/cycle detection; the staged clone is now an attacker-controlled production surface. A directory symlink to an ancestor (git preserves symlinks) → unbounded async recursion → gate hangs, `finally` cleanup never runs; symlinked SKILL.md also reads outside the staged clone. Both judges independently flagged. FIXED in design.md (D9 symlink-safe walk + D10 revised skill-scanner.ts claim, File Changes, Testing 15). |
| JD-004 | judgment-day | `src/ui/AutoSkills.tsx:31-36`; `src/cli/dispatch/skills-cmd.tsx:63` | WARNING | info | real | Auto-install gate CLI-unreachable (sameDir wiring) — NOT a spec violation (lib-level scenarios 7-9 hold; documented in design.md:27). Judge B marked refuted/SUGGESTION. Merged as documented reachability gap, not a fix. |
| JD-005 | judgment-day | `design.md:12` × `src/lib/skill-scanner.ts:520` | SUGGESTION | info | theoretical | Full-tree walk on every `plugin add`; file count unbounded (only node_modules/.git skipped) → latency risk for vendored plugins; cost, not correctness (byte-identical behavior preserved). |
| JD-006 | judgment-day (re-judge Round 2) | `D1` + `D8` × `src/lib/agent-skills.ts:148`; `src/lib/plugin.ts:192` | CRITICAL | fixed | real | Scan-scope / install-scope mismatch in both gated install paths: gates scan only manifest-declared skills while `fs.copy(sourceDir, destDir)` (agent-skills.ts:148) / `fs.move(tmpDir, destDir)` (plugin.ts:192) install the ENTIRE tree — an attacker declares a clean skill and hides a malicious payload (undeclared SKILL.md, executable, hook, asset) elsewhere in the tree, installed un-scanned (reopens JD-001 class). FIXED in design.md via declare-and-enforce (declared-set == copied-set): new SKILL.md-only coverage walk `scanSkillsWithCoverage` (D1 chosen row, D8 gate steps, Data Flow, Interfaces, File Changes, Testing 16); import validation extended with non-empty `agentManifest.skills` + `entry.path` realpath containment (D8); PLUGIN.md/README still never scanned (JD-002 intact); new refusals are block-level — `force` never lifts them; rejected alternatives (option 1 restrict-install-scope, option 3 scan-only) documented in D1. |

## Adversarial verification

Judgment Day two-judge convergence satisfies adversarial verification — no review-refuter fan-out (per skill).

- JD-001: CONFIRMED (both judges, same defect, independent sweep) → enters fix → re-review loop.
- JD-002/003: WARNING (info) — reported once; both amended into design.md in round 1 fix pass (user discretion per verdict), status → fixed.
- JD-004: SUGGESTION/refuted — reported once as info.
- JD-005: SUGGESTION (info) — reported once.

## Verdict

JUDGMENT: APPROVED with 1 confirmed CRITICAL (JD-001) — fix required before sdd-tasks. Warnings JD-002/JD-003 are candidate design amendments at user discretion (not protocol fixes).

## Re-judge (Round 2)

- JD-006: CONFIRMED CRITICAL (scan-scope / install-scope mismatch in both gated install paths) → design amended (declare-and-enforce coverage walk, D1/D8/Data Flow/Interfaces/File Changes/Testing 16) → status: **fixed**. No new problem surfaced by the fix; no spec change required (scenarios 1-12 remain satisfiable — compliant packages still install byte-identically).

## Re-judge (Round 3 — final scoped re-review after fix Round 2)

| id | lens | location | severity | status | assessment | evidence |
|----|------|----------|----------|--------|------------|----------|
| JD-007 | judgment-day | design.md D1/D9/D11 `scanSkillsWithCoverage` × `src/lib/plugin.ts:192`; `src/lib/agent-skills.ts:148` | CRITICAL | fixed | real | Coverage walk skips `node_modules`/`.git` (inherited from skill-scanner.ts:520, cost rationale) but install is whole-tree (`fs.move` plugin.ts:192 / `fs.copy` agent-skills.ts:148; fs-extra preserves symlinks). (a) A committed `node_modules/<pkg>/SKILL.md` lands in PLUGINS_DIR un-scanned and un-declared — JD-006 class reopens; (b) symlinked `SKILL.md`/dirs are lstat-skipped (never collected undeclared, never scanned) yet install as live symlinks, dereferencing post-install to attacker-chosen content. The design's own unconditional claims (D1 "every skill-shaped file", D8 "any SKILL.md outside declared set → refuse") contradict the D9 exemption. Both judges independently confirmed. FIXED in design.md (Round 3, user-authorized override): new D11 — coverage walk has NO subtree exemptions (`node_modules`/`.git` visited; visit set == install footprint) and ANY symlink (file or dir) → manifest-integrity refusal, block-level, force never lifts; D1/D8 claims now literally true for the installed tree; D9 revised skip→refuse (lstat/visited-set/realpath containment retained as defense-in-depth, JD-003); `scanSkillsWithCoverage` gains `symlinks[]` output; Data Flow, Interfaces (refusal classes), File Changes, Testing rows 15-17, Migration and Non-goals residual all amended. No spec change required (scenarios 1-12 hold; clean installs remain byte-identical); `force?: boolean` semantics unchanged. Residual (non-SKILL.md assets) stays documented P-1; NOT the same as JD-007 (skill-shaped files are within the scanner content model). |
| JD-001 | — | — | CRITICAL | verified | — | D8 gate placement finally verified correct (both judges round 3): gate before fs.remove/fs.copy, validation extension concrete against real types, dryRun honored, force threading additive, existing install preserved. |
| JD-002 | — | — | WARNING | verified | real | PLUGIN.md/README never scanned by final design (declared scanSkillFile + SKILL.md-only coverage); false-block lockout eliminated; Testing 14/16(e) bind. |
| JD-003 | — | — | WARNING | verified | real | lstat + visited-set + realpath containment terminate cycles and prevent out-of-clone reads; symlink-skip meets JD-003's scan-time asks (its install-side cost = JD-007). |
| JD-006 | — | — | CRITICAL | NOT fully verified | real | Normal-tree undeclared SKILL.md now refused (block-level, force never lifts); but node_modules/.git + symlink exemptions keep the smuggling vector open → folded into JD-007. |

## Terminal state

**JUDGMENT: APPROVED** — Round 3 fix (user-authorized override of the 2-round convergence budget) applied to design.md: JD-007 closed via D11 (no walk exemptions + symlink refusal) with coherent amendments across D1, D8, D9, D10, Data Flow, Interfaces, File Changes, Testing rows 15-17, Migration and Non-goals. All confirmed CRITICAL findings (JD-001, JD-006, JD-007) are **fixed**; warnings JD-002/JD-003 verified in the final design. No new problem surfaced and no spec change required (scenarios 1-12 remain satisfiable; compliant packages still install byte-identically). Ready for sdd-tasks.

## Final verification (post-R3, both judges, parallel)

**VERDICT: CLEAN — all ledger findings (JD-001, JD-002, JD-003, JD-006, JD-007) verified fixed. Nothing new.** No CRITICAL/BLOCKER survived. D11 verified: visit set == install footprint (walk visits node_modules/.git; git cannot track into `.git` and fresh shallow clones carry 0 symlinks / 0 SKILL.md basenames — empirically checked by Judge B via `git clone --depth 1`); any symlink → manifest-integrity refusal before any content read (Data Flow orders walk before declared scan); PLUGIN.md never content-scanned (JD-002); lstat/visited-set/realpath retained as defense-in-depth (JD-003); clean installs byte-identical (Testing 17).

Info notes carried into sdd-tasks as implementation-time cautions (never block, no round):
- **JD-008** (SUGGESTION, info): D11 premise "a legit package ships no symlinks" is factually weak — symlinks are routine in real repos (npm `node_modules/.bin/*`, agent skill dirs). A legit symlinked package would be force-proof refused with no escape hatch. No current user impact (registry empty); the strictness is deliberate — wording should name the tradeoff in Migration.
- **JD-009** (SUGGESTION, info): `auto-skill-install` copies whole skill **folders** (`fs.copy(sourcePath, targetPath)`, auto-skill-install.ts:99) while D4's gate scans only folder-root SKILL.md — a nested `SKILL.md` inside the skill folder lands un-scanned. Weak attacker model (source is user's own local skills dir; destination ≠ PLUGINS_DIR); wording must not mislead implementation.
- **JD-010** (SUGGESTION, info): Testing 17 parenthetical "(current installed base) WITHOUT node_modules/.git content" is factually wrong — every plugin-add install carries the clone's root `.git` via `fs.move`. The requirement itself is correct; the parenthetical could mislead the implementer into skipping the `.git` walk.

## Implementation judgment (post-apply, both judges, parallel — hot path 2 sweeps)

**VERDICT: NO BLOCKER/CRITICAL — implementation matches the final design (D1–D11) on all 12 spec scenarios; closes JD-001/JD-006/JD-007 as designed.** Judges independently ran the full suite (1763 pass / 2 pre-existing skips, typecheck/lint clean), read the whole diff, and Judge B live-executed `scanSkillsWithCoverage` on fixtures. Fail-closed integrity, force semantics, coverage walk (no exemptions), gate placement, containment, UX/reports, regressions (clean byte-identical installs, 3-arg callers compile) all PASS.

### Implementation findings (WARNING/SUGGESTION — info, never block)

| id | location | severity | assessment | evidence |
|----|----------|----------|------------|----------|
| JD-011 | `src/lib/skill-scanner.ts:604` × `:655` | WARNING | real → fixed | Declared-set membership is exact-case (`declaredFiles.add(...,"SKILL.md")`) while the walk collects case-insensitively (`toLowerCase() === "skill.md"`). A legitimately declared lowercase `skill.md` is collected as skill-shaped, fails membership → `undeclared` → block-level refusal that `--force` never lifts, while its declared scan reports `unscannable`. Verified live: `skills/alpha/skill.md` → `declared:[unscannable]`, `undeclared:["alpha/skill.md"]`. Permanent hard lockout for a declared skill with misleading message. Fix: seed `declaredFiles` with both basenames or compare lowercased. (Judge B only; Judge A did not flag.) FIXED in F1: both-basenames seeding + case-tolerant `declaredSkillFileOnDisk` resolution; test binds declared lowercase `skill.md` → `declared` (scanned pass), not in `undeclared`. |
| JD-012 | `src/lib/agent-skills.ts:269-299` × `agent-skills.test.ts:21`; `auto-skill-install.test.ts:73-86` | WARNING | real → fixed | Test gap vs design Testing 16(c) + scenarios 7-9, and a FALSE claim in `apply-progress.md` §5.3 ("containment (../../, absolute)"). `skillPathContained` has ZERO direct tests (no `path:"../../x"`/absolute passed to import gate; `fs.realpath` identity-mocked so realpath branch is dead in that suite). SameDir "scan not called" also unasserted. Implementation IS correct (verified by inspection) — regression protection missing. FIXED in F1: import-entrypoint containment tests (`../../`, absolute → refused before scan+copy, scanner never called), realpath-escape branch with non-identity realpath mock, sameDir "scanner never called" assertion, and apply-progress §5.3 claim corrected. |
| JD-013 | `src/lib/skill-scanner.ts:617-622, 624-629, 633-638` | WARNING | theoretical → fixed | Walk swallows I/O failures (`catch { return; }` on realpath/readdir, `catch { continue; }` on lstat) → an unlistable subtree is treated as empty and install proceeds — deviation from "no path swallows errors". Not reachable via git content (git cannot create EACCES dirs); limited to fs-full/race. Fix: record failing path as manifest-integrity refusal. (Judge A = JD-101, same finding.) FIXED in F1: `SkillCoverageScan.errors: string[]` populated on realpath/readdir/lstat failure; both gate call sites refuse on `errors.length > 0` (before symlink/undeclared checks, force never lifts); walk tests bind readdir+lstat failure recording; call-site refusal tests added in plugin.test.ts + agent-skills.test.ts. |
| JD-014 | `src/lib/plugin.ts:241`; `src/lib/agent-skills.ts:220` | SUGGESTION | theoretical → fixed | `formatBatchReport(gate.rejected)` receives only rejected results while design D6 says `formatBatchReport(scanResults)` (full declared set, design.md:40). When 1 of 3 declared skills blocks, header reads "Scanned: 1 skill(s)" — undercount. Spec scenario 10 still satisfied. Fix: pass `coverage.declared`. FIXED in F1: both call sites pass the full declared set (`coverage.declared`/hoisted `declaredResults`); lead line still names rejected count. |
| JD-103 | `src/lib/agent-skills.test.ts` `mockSuccessfulImport` (~:369-380) | SUGGESTION | info → fixed | "Existing install preserved on refused import" not genuinely bound: `mockFs.pathExists` returns false for destDir in every refusal test, so `expect(mockFs.remove).not.toHaveBeenCalled()` is vacuously true. Code order is correct (refusals return before fs.remove at :233) but the guarantee is not exercised. Fix: make pathExists true for existing PLUGINS_DIR/<name> in one refusal test, assert remove+copy not called. (Judge A only; Judge B = part of JD-012 cluster.) FIXED in F1: dedicated test with `pathExists(destDir)` true asserts both `fs.remove` and `fs.copy` never called. |

### Implementation verdict

**JUDGMENT: APPROVED** — no CRITICAL/BLOCKER. Warnings/suggestions (JD-011, JD-012, JD-013, JD-014, JD-103) reported once as info per protocol. Candidate user-authorizable fixes (like JD-002/003 in design round): JD-011 (case-mismatch lockout — real user impact, cheap), JD-012 (add containment + sameDir tests, fix apply-progress claim), JD-013 (errors[] refusal on walk I/O failure), JD-014 (report undercount), JD-103 (bind existing-install-preserved). None block apply→verify.

## Fix Round F1 (user-authorized override — all 5 info findings fixed) + re-review

**User decision**: "fix a todo" — authorize fixing JD-011, JD-012, JD-013, JD-014, JD-103. Fix agent applied 3 commits on `feat/skillguard-runtime-gate`: `e7e8f408`, `0d84d845`, `35d77446` (10 commits total on branch). Ledger statuses above set to `fixed`; apply-progress.md corrected (§5.3 false containment claim → tests actually added in F1) and merged with F1 results; engram apply-progress upserted.

**F1 test results (real exit codes)**: `pnpm test` 96 files, 1772 pass / 2 pre-existing skips (+9 new tests); `pnpm typecheck` 0; `pnpm typecheck:test` 0; `pnpm lint` 0 (1 pre-existing unrelated warning `ci-hooks.test.ts:1171`); pre-commit CI on all 3 commits.

**Re-review (post-F1, both judges, parallel, scoped to `git diff 97abbfcc..HEAD` only — full implementation diff forbidden):**

**VERDICT: CLEAN — all five F1 findings verified fixed; zero new BLOCKER/CRITICAL on fix-touched lines.** Per-finding verification (both judges independently, live repro by B):
- JD-011 verified: seeded both basenames + `declaredSkillFileOnDisk` case-tolerant resolution (`skill-scanner.ts:621-622`, `:708`, `:726-732`); declared lowercase `skill.md` → `declared[pass]`, `undeclared=[]` (live repro); undeclared lowercase still refused (CASE3 preserved).
- JD-012 verified: `it.each(["../../outside","/absolute/outside"])` import-entrypoint refusals asserted scanner+copy never called; realpath branch exercised with non-identity mock; sameDir "scanner not called" asserted; false apply-progress claim corrected.
- JD-013 verified: `SkillCoverageScan.errors: string[]` (`:578`) recorded on realpath/readdir/lstat failures (`:637-666`); both gate call sites refuse on `errors.length > 0` FIRST, before symlink/undeclared/gate — unconditional, force never lifts (`plugin.ts:215-220`, `agent-skills.ts:202-207`); spy-based EACCES tests bind recording + call-site `force:true` refusal.
- JD-014 verified: `formatBatchReport` receives full declared set (`plugin.ts:203/234/254` hoisted `declaredResults`; `agent-skills.ts:232` `coverage.declared`); lead line still names rejected count.
- JD-103 verified: `pathExists(destDir)→true` refusal test asserts both `fs.remove` and `fs.copy` never called (`agent-skills.test.ts:412-440`).

### F1 info registrations (never block, no round)

| id | location | severity | assessment | evidence |
|----|----------|----------|------------|----------|
| JD-F1-N1 | `src/lib/skill-scanner.ts:621-622,708` | WARNING | real-ish, inert | A declared dir shipping BOTH `SKILL.md` (clean) and lowercase `skill.md` (malicious): only the canonical is content-scanned; the lowercase variant installs un-scanned. Pre-fix this exact state was refused — but that refusal WAS the JD-011 lockout defect, so the trade is deliberate (canonical preferred; P-1 residual class). Inert: no runtime consumer loads lowercase `skill.md` from PLUGINS_DIR (discoverSkills parsing.ts:178, codex-export.ts:96, skill-publish.ts:56, claudemd.ts:88/154, auto-wire.ts:122, manifest generation agent-skills.ts:63-85 read exact-case only), so JD-006/JD-007 "activatable malicious skill" harm is not reachable through this corner. `node_modules`/undeclared-dir smuggling still fully refused (CASE3 live verified). |

### F1 re-review verdict

**JUDGMENT: CLEAN / APPROVED** — zero BLOCKER/CRITICAL confirmed after adversarial re-review of F1; the fix loop closes on round 1 of the implementation judgment (no confirmed findings survived). JD-F1-N1 persists as info only. Next: sdd-verify against spec (12 scenarios), then single PR to main (size:exception) with gates.

## Pre-PR full 4R review (hot path: security + >400 lines; lenses in parallel on `git diff main...HEAD`, 24 files +2542/−23)

**VERDICT: NO BLOCKER/CRITICAL from any lens — PR-ready subject to the case-variant lockout decision below.** All four lenses independently verified the fail-closed contract holds (force lifts ONLY unscannable; errors/symlink/undeclared/containment refusals block-level; finally cleanup leak-free; refusals before fs.remove/fs.move; no new dependency surface).

### 4R findings ledger (WARNING/SUGGESTION — info, never block)

| id | lens | location | severity | status | assessment | evidence |
|----|------|----------|----------|--------|------------|----------|
| R1-001 / R3-001 / R4-001 | risk+reliability+resilience | `skill-scanner.ts:684` (case-insensitive collection) × `:621-622` (exact-case seeding) / `:726-733` | WARNING | fixed | **real, empirically verified (2 live repros)** — FIXED in F2: declared-membership is now decided by the declared DIRECTORY (case-insensitive for any case fold), and `declaredSkillFileOnDisk` resolves any fold via a case-insensitive readdir fallback; tests bind declared dirs with `Skill.md`/`SKILL.MD` → `declared` (scanned, verdict pass) + `undeclared: []`, and an undeclared dir's `Skill.md` → still `undeclared` (CASE3 smuggling refusal intact); symlink skip + fail-closed `unscannable` on missing preserved | The JD-011 F1 fix closed ONLY the full-lowercase `skill.md` variant. Any other case fold (`Skill.md`, `SKILL.MD`, `skill.MD`) of a DECLARED skill is collected case-insensitively (:684) but misses the exact-case declared set (:621-622) → `undeclared` → block-level refusal force never lifts, while `declaredSkillFileOnDisk` (:726) reports the same declared entry `unscannable`. Verified live: fixtures with `Skill.md`/`SKILL.MD` → `undeclared=[...]`, `declared=[unscannable]`. A legit package (plausible from case-insensitive-FS authors) becomes un-installable forever; spec scenario 6 ("clean installs unchanged") violated for this class. Only recovery: rename upstream. Same defect class JD-011's comment (:614-618) says it closes. |
| R1-002 | risk | `agent-skills.ts:182-188,244-250` (pre-existing on main, PR-adjacent) | WARNING | fixed | pre-existing, not introduced by diff — FIXED in F2: `importAgentSkillsPackage` now validates `agentManifest.name` (absolute, `/`/`\` separators, `..`, empty/`.`/`..`-shaped → refused) BEFORE any `destDir` use / `fs.remove` / `fs.copy`, with a gate-style `skillguard: … invalid manifest name (manifest-integrity, force never lifts)` error; tests bind `"../../escape"`, `"/abs/path"`, `"a/b"` (+ backslash, `..`, `.`, whitespace) → refused with remove+copy never called against a live existing install; same-trust comment notes name is attacker-influenced from a registry package | `plugin import` computes `destDir = path.join(PLUGINS_DIR, agentManifest.name)` with NO name validation, then `fs.remove`+`fs.copy` — a hostile `skills.json` `name` (`"../../.bashrc"`) performs arbitrary-path delete/copy outside PLUGINS_DIR. Pre-dates this diff (`git show main` confirms) but sits exactly on the privilege boundary this change claims to harden; the gate checks `entry.path` escapes but never the `name` that determines the write/delete destination. Flagged for the ledger + a follow-up candidate; not blocking this PR. |
| R2-001 | readability | `plugin.ts:204-256` × `agent-skills.ts:194-242` | WARNING | info | duplication | Entire gate refusal chain (~50 lines) copy-pasted across the two package entrypoints: identical errors→symlinks→undeclared integrity checks, byte-identical error templates, identical report lead. D2 hoisted ONLY `evaluateInstallGate`; refusal policy + message-building stayed duplicated. Future wording/integrity-class change must be made twice or entrypoints silently diverge in the exact user-facing refusal surface this change makes deterministic. |
| R2-002 | readability | `skill-scanner.ts:740-772` × `agent-skills.ts:281-311` | WARNING | info | duplication | The containment policy (JD-003, security-critical) is implemented twice with same algorithm but different error shapes (`resolveContained` throws vs `skillPathContained` returns `{ok:false}`). A hardening applied to one copy leaves the other with the weaker check while both claim the same JD-003 invariant. |
| R2-003 | readability | `commands/plugin.ts:35-39` × `:293-296`; `Plugin.tsx:89,150` | WARNING | info | parameter asymmetry, sanctioned by D5 | Same concept (`force`) in different shapes: `runPluginAdd(..., options: { force?: boolean } = {})` vs `runPluginImport(..., force = false)`. Fossilized in tests (`{force:true}` vs `true`). Maintainer threading force through a new path can mirror the wrong sibling. |
| R3-002 | reliability | `skill-scanner.ts:621-622,708` | SUGGESTION | info | residual (same class as JD-F1-N1) | Declared dir with BOTH `SKILL.md` + `skill.md` → only canonical scanned, lowercase twin installs un-scanned. Low likelihood, hostile-usable; JD-F1-N1 already documented. |
| R3-003 | reliability | `vitest.config.ts:5-20` / `.github/workflows/ci.yml:43` | WARNING | fixed | pre-existing config, no forbidOnly — FIXED in F2: `vitest.config.ts` gains `allowOnly: false` (vitest 4's key; the jest-style `forbidOnly` key does not exist in vitest 4 and is silently ignored — verified empirically that `.only` still passed with it), so a committed `test.only`/`describe.only` FAILS both CI (`test:coverage` uses the same config) and local runs; suite without `.only` unchanged; ci.yml untouched (config is the authoritative fix) | CI runs `pnpm test:coverage` with NO `forbidOnly` — CI can pass with a committed `test.only`/`describe.only`. This diff adds ~1,100 test lines, the exact surface for a debug `.only` remnant; nothing in CI fails. Config predates the diff but integrity gap is real for this test-heavy PR. |
| R4-002 | resilience | `ui/Plugin.tsx:83-150`, `ui/AutoSkills.tsx:59`, `index.tsx:44-95` | WARNING | info | pre-existing, D6 documents exit(1) as V-2 follow-up | Every skillguard refusal ends with exit code 0 (Plugin.tsx never sets process.exitCode; AutoSkills bare `exit()` → ink default 0). A scripted consumer sees a green exit while the fail-closed gate silently prevented install — a `block` is indistinguishable from success to automation. Pre-dates diff (all plugin failures exit 0); known bound. |

### 4R verdict

**JUDGMENT: NO BLOCKER/CRITICAL — merge-blocking bar not met; PR-ready except one user decision.** Only candidate for the fix loop: R1-001/R3-001/R4-001 (case-variant lockout, empirically verified, same class as the user-authorized JD-011 fix, WARNING severity because fail-closed holds — nothing unsafe installs). All other findings are info (duplication, pre-existing surfaces, config) and never re-review. No refuter fan-out needed (no BLOCKER/CRITICAL candidates). Ledger persists pre-PR; fix decision deferred to the user (pattern: user authorized "fix a todo" for the JD-011 class once already).

## Fix Round F2 (user-authorized: case-variant + forbidOnly + name-validation) + scoped re-review

**User decision**: "Fix case-variant + forbidOnly + follow-up" — authorize fixing R1-001/R3-001/R4-001 (case-variant lockout), R1-002 (manifest name → destDir traversal), R3-003 (forbidOnly). Fix agent applied 4 commits: `d073716d` (case-variant: membership by declared DIRECTORY + case-insensitive readdir fallback), `cb8b49db` (vitest `allowOnly: false` — vitest 4.1.6 has NO `forbidOnly` key; verified empirically that `forbidOnly` is silently ignored and `.only` still passes, while `allowOnly: false` fails on committed `.only` both in CI and locally; config-level is authoritative and applies to `test:coverage` too), `7d275839` (manifest name validation in `importAgentSkillsPackage` before ANY fs mutation), `ed28ec6c` (ledger + apply-progress). **Suite: 96 files / 1783 pass / 2 pre-existing skips (+11 tests) — typecheck ×2, lint, build all 0.**

**Re-review (post-F2, reliability + risk lenses in parallel, scoped to `git diff 451a1712..HEAD` only):**

**VERDICT: CLEAN — all three F2 findings verified fixed with live-executed evidence; zero new BLOCKER/CRITICAL on fix-touched lines.** Per-finding:
- R1-001/R3-001/R4-001 verified: membership by declared dir (`skill-scanner.ts:624`, `:689-694`), any-fold file resolution (`declaredSkillFileOnDisk` `:734-752`, case-insensitive readdir fallback `:745`); `it.each(["Skill.md","SKILL.MD","skill.MD"])` → declared pass, `undeclared: []`; CASE3 (non-declared dir fold → undeclared), symlink skip, fail-closed `unscannable` all live-verified.
- R1-002 verified: validation at `agent-skills.ts:147-160` placed BEFORE destDir (`:212`), fs.remove (`:269-271`), fs.copy (`:274`); refuses `../../escape`, `/abs/path`, `a/b`, `a\b`, `..`, `.`, whitespace, empty → success:false with `invalid manifest name (manifest-integrity, force never lifts)`; test non-vacuous (pathExists→true for would-be destDir; regression would fire fs.remove and fail).
- R3-003 verified: `vitest.config.ts:20` `allowOnly: false`; probe: committed `it.only` → exit 1 (both with and without CI env); plain suite unchanged; ci.yml `test:coverage` reads same config.

### F2 info registrations (never block, no round)

| id | location | severity | assessment | evidence |
|----|----------|----------|------------|----------|
| R1-F2-N1 | `skill-scanner.ts:691` × `:791` (`resolveContained` returns manifest-case `entryAbs`) | WARNING | real, verified live → fixed | DIR-NAME case fold still locks out (the fix's "any case fold" contract is file-only). Manifest declares `skills/Alpha`, disk tree has `skills/alpha` → `declared:[unscannable]`, `undeclared:[...alpha/SKILL.md]` → block-level refusal force never lifts. Same shape as R1-001 for packages authored on case-insensitive FS installing on case-sensitive targets (Linux CI/servers). `errors: []` so errors-refusal doesn't catch it. Both lenses converged. FIXED in F3: declared-dir membership is lowercased on both sides (`declaredDirAbsLower`; walk checks `path.dirname(resolved).toLowerCase()` against it) AND the declared scan resolves each declared dir to the real on-disk dir the walk visited (exact-case real dir → case-fold match → manifest-path fallback keeps a truly-missing declared dir failing closed as `unscannable`); test binds `skills/Alpha`+`skills/beta` declared vs disk `skills/alpha`+`skills/beta` → both declared scans pass reading real on-disk paths, `undeclared: []`, no errors (RED pre-fix verified live); CASE3 `evil/Skill.md` refusal test kept + passing; symlink skip and fail-closed `unscannable` preserved; cross-platform: on case-insensitive FS both spellings already coincide, lowercased comparison is a no-op there. |
| R1-F2-N2 | `agent-skills.ts:149` | WARNING | cosmetic → fixed | `pluginName.trim()` without `typeof` guard → hostile non-string `name` (`{"name":123}` — truthy, passes the required-fields check) throws TypeError instead of clean refusal. Fail-safe: propagates to UI "Fatal error"; install still refused, zero fs mutation, no security harm. (Risk lens only.) FIXED in F3: `typeof pluginName !== "string"` guard placed BEFORE `.trim()` → non-string truthy `name` refuses with the same `invalid manifest name (manifest-integrity, force never lifts)` message, `success:false`, no fs mutation; test binds `{"name":123}` (number) → clean refusal, `fs.remove`+`fs.copy` never called (no TypeError). |

### F2 re-review verdict

**JUDGMENT: CLEAN — fix loop closed at round 2 of the 4R review (convergence budget exhausted); anything further is user-authorized override, never automatic.** R1-F2-N1 registered as info (same WARNING class as R1-001, not a new BLOCKER/CRITICAL); does not re-open the round. PR-ready from a review standpoint; R1-F2-N1 deferred to the user (pattern: user authorized the two prior case-class fixes).

**FIX ROUND F2 (user-authorized, pre-PR):** the user authorized fixing the five actionable findings — R1-001/R3-001/R4-001 (case-variant lockout, the main fix), R1-002 (manifest-name arbitrary path delete/copy, pre-existing on main but on this PR's boundary), R3-003 (forbidOnly config). All fixed as logged above (statuses → `fixed`); 3 code commits (`d073716d`, `cb8b49db`, `7d275839`) + docs commit; pre-commit CI green on each; full suite re-run: **96 files / 1783 pass / 2 pre-existing skips** (+11 new tests), `pnpm typecheck` 0, `pnpm typecheck:test` 0, `pnpm lint` 0 (1 pre-existing unrelated warning `ci-hooks.test.ts:1171`), `pnpm build` 0. Info-only findings (R2-001/002/003 duplication, R3-002 residual, R4-002 exit-code bound) remain untouched — never block, not part of this fix round. No new problem surfaced by the fixes; no new ledger rows added.

## Fix Round F3 (user-authorized override — third instance of the case-lockout class + typeof guard) + scoped re-review

**User decision**: surgical fix round F3 on the SAME lockout class (the third override): authorize fixing R1-F2-N1 (DIR-NAME case fold lockout) and R1-F2-N2 (non-string manifest name → TypeError). Fix agent applied 2 code commits + docs commit; pre-commit CI (lint+build) green on each. **Suite: 96 files / 1785 pass / 2 pre-existing skips (+2 tests) — `pnpm typecheck` 0, `pnpm typecheck:test` 0, `pnpm lint` 0 (1 pre-existing unrelated warning `ci-hooks.test.ts:1171`), `pnpm build` 0.**

- R1-F2-N1 (WARNING, real, verified live) — **FIXED**: `scanSkillsWithCoverage` now makes declared-dir membership case-insensitive on the DIRECTORY name, the robust closure rather than more file special-casing: (1) walk-side membership compares lowercased paths (`declaredDirAbsLower` set; check is `declaredDirAbsLower.has(path.dirname(resolved).toLowerCase())`); (2) scan-side, each declared dir is resolved to the REAL on-disk dir the walk visited (two-tier: exact-case real dir → case-fold match → manifest-path fallback, so a truly missing declared dir still fails closed as `unscannable`), and only the real path is handed to `declaredSkillFileOnDisk`/`scanSkillFile` — no casing is ever invented for file access. Preserved: CASE3 (skill-shaped file outside ANY declared dir — any casing — still `undeclared`), symlink skip on the resolved path, fail-closed `unscannable`, cross-platform determinism (on case-insensitive FS both spellings already coincide — lowercasing is a no-op there). RED proof: pre-fix code failed the new binding test with exactly the finding's lockout (`undeclared` caught `.../skills/alpha/SKILL.md`); post-fix green. Tests: `treats a declared dir whose on-disk NAME differs in case as declared and scans its real path — not undeclared (R1-F2-N1)` (declares `skills/Alpha`+`skills/beta`, disk `skills/alpha`+`skills/beta` → both declared scans `pass` on real paths, `undeclared: []`, `symlinks: []`, `errors: []`); CASE3 `evil/Skill.md` test kept and passing.
- R1-F2-N2 (WARNING, cosmetic) — **FIXED**: `importAgentSkillsPackage` guards `typeof pluginName !== "string"` BEFORE the `.trim()` check → a hostile non-string truthy `name` (`{"name":123}`) refuses cleanly with the same `invalid manifest name (manifest-integrity, force never lifts)` message, `success:false`, zero fs mutation — no TypeError. Test: `refuses a non-string manifest name (a number) cleanly — no TypeError, no remove/copy (R1-F2-N2)` with `pathExists` true for the would-be destDir (non-vacuous — a regression would fire `fs.remove` and fail).

### F3 re-review verdict

**JUDGMENT: CLEAN — both F3 findings verified fixed with live-executed evidence (RED pre-fix repro for N1); zero new BLOCKER/CRITICAL on fix-touched lines; fix loop closed at round 3.** No new ledger rows added (nothing new to log as a finding). One carried residual REPORTED (not logged, per protocol): on a case-sensitive FS, a tree containing BOTH a declared dir and a case-variant twin dir (`skills/alpha` declared + `skills/Alpha` undeclared sibling) now passes membership for the twin's SKILL.md (lowercased membership can no longer distinguish them) — pre-F3 the twin's file was refused as undeclared. This is the SAME documented residual family as R3-002/JD-F1-N1 (low likelihood, hostile-usable; inert — no runtime consumer reads a case-variant twin: consumers construct exact-case manifest paths only), and it is the deliberate trade of the prescribed lowercased-membership closure; the declared scan still prefers the exact-case real dir when it exists. If the user wants that corner closed too, it needs a future round (e.g. reject declared dir names that case-collide with an on-disk sibling).